import 'package:flutter/material.dart';

class KonversiWaktu extends StatelessWidget {
  const KonversiWaktu({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
